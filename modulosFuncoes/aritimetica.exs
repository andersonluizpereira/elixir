defmodule Mathc do
    def multiplicacao(a,b) do
        IO.puts "#{a * b}"
    end
    def adicao(a,b) do
        IO.puts "#{a + b}"
    end
    def divisao(a,b) do
        IO.puts div 10,2
    end
    def resto(a,b) do
        IO.puts rem 10,2
    end
end